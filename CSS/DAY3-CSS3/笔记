快速生成HTML结构语法

    生成多个相同标签，加*
        div*3(生成三个div)

    父子级关系的标签，用>
        ul>li
        
    兄弟关系的标签，用+
        div+p

    生成带有类名或者id名字的，直接写.demo 或者 #two 回车即可，
    默认是生成div，若要指定生成标签
        span.demo/p#two 回车即可

    生成的div类名有顺序，用自增符号$
        .demo$*3

    在生成标签内部写内容可用{}表示
        div{我是帅哥}*3 div{$}*3

